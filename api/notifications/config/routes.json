{
  "routes": [
    {
      "method": "GET",
      "path": "/notifications",
      "handler": "Notifications.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/notifications/count",
      "handler": "Notifications.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/notifications/:_id",
      "handler": "Notifications.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/notifications",
      "handler": "Notifications.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/notifications/:_id",
      "handler": "Notifications.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/notifications/:_id",
      "handler": "Notifications.destroy",
      "config": {
        "policies": []
      }
    }
  ]
}