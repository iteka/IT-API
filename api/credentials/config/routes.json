{
  "routes": [
    {
      "method": "GET",
      "path": "/credentials",
      "handler": "Credentials.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/credentials/count",
      "handler": "Credentials.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/credentials/:_id",
      "handler": "Credentials.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/credentials",
      "handler": "Credentials.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/credentials/:_id",
      "handler": "Credentials.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/credentials/:_id",
      "handler": "Credentials.destroy",
      "config": {
        "policies": []
      }
    }
  ]
}
